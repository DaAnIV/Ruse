[workspace]
members = [
    "lib/*",
    "benchmarks"
]
resolver = "2"

[workspace.dependencies]
tracing = { version = "*", features = ["release_max_level_debug"] }

[profile.release]
lto = "thin"

[profile.bench]
debug = true
strip = false

[profile.profiling]
inherits = "release"
debug = true
